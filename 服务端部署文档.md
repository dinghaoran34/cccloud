# CC网盘 服务端部署文档

> 适用对象：CC网盘网站（Web）与安卓原生 App（v2.x）共同使用的服务端源码。
> 服务端为单进程 Node.js 应用，对外统一建议通过 Nginx 提供 HTTPS；安卓 App 已强制全链路 HTTPS。

---

## 1. 系统架构

```
安卓App / 浏览器
      │  HTTPS (443)
      ▼
  Nginx（TLS 终结 + 反向代理）
      │  HTTP 转发到本机 127.0.0.1:9178
      ▼
 Node.js 服务（server.js，端口 9178）
      │
      ├── SQLite：CC网盘主库 teamcloud.db
      └── S3/COS 对象存储：网盘文件（文件不落服务器本地磁盘）
```

- Web 页面：EJS 模板（`views/`）＋ 静态资源（`public/`）。
- 安卓 App JSON 接口：`/api/*`（注册/登录/网盘/团队/账户）。
- 文件直传：浏览器/App 拿预签名地址直接 PUT 到 S3，不经 Node 转发大文件。

---

## 2. 目录结构（源码内关键路径）

```
server.js              # 主服务入口（全部配置项见下文第 4 节）
package.json           # 依赖清单
s3store.js             # S3/COS 对象存储封装（读环境变量）
views/                 # Web 页面模板（modern/兼容/手机版 三套）
public/                # 静态资源（css/js/图标）
用户协议.txt           # 用户协议文本（GBK，服务端读取转 UTF-8）
```

---

## 3. 环境要求

- Ubuntu 20.04 / 22.04（或兼容 Linux）
- Node.js **18 LTS 及以上**（推荐 20 LTS）
- Nginx（用于 HTTPS 反向代理）
- 可公网访问的域名，且已配置好 TLS 证书（推荐 certbot/Let's Encrypt）
- 一个兼容 S3 协议的存储桶（默认 RainS3 `https://cn-nb1.rains3.com`，也兼容 MinIO/腾讯 COS 等，把 Endpoint 换成对应地址即可）

---

## 4. 环境变量与顶部配置

### 4.1 环境变量（启动前设置）

| 变量 | 必填 | 说明 |
|---|---|---|
| `TEAMCLOUD_S3_ACCESS_KEY` | 是 | 对象存储 Access Key |
| `TEAMCLOUD_S3_SECRET_KEY` | 是 | 对象存储 Secret Key |
| `TEAMCLOUD_S3_ENDPOINT` | 否 | 默认 `https://cn-nb1.rains3.com` |
| `TEAMCLOUD_S3_BUCKET` | 否 | 默认 `teamcloud` |
| `PORT` | 否 | 监听端口，默认 `9178`（与安卓/前端反向代理约定） |
| `HOST` | 否 | 监听地址，默认 `127.0.0.1`（建议保持，仅本机 Nginx 访问） |

### 4.2 `server.js` 顶部源码配置

| 位置 | 常量 | 说明 |
|---|---|---|
| 第 17-18 行 | `PORT` / `HOST` | 监听端口与地址（可被环境变量覆盖） |
| 第 55-59 行 | `GEETEST_*` | 极验**第四代**，Web 网页登录/注册使用，登录/注册各自独立 ID/KEY |
| 第 108-111 行 | `GT3_LOGIN_*` / `GT3_REGISTER_*` | 极验**行为验证3.0**，安卓 App 登录/注册使用，登录/注册各自独立 ID/KEY |
| 第 353 行 | `session secret` | Session 签名密钥，生产环境请改为随机长字符串 |

> 说明：若填写为空，服务端对对应场景“有验证参数则验证、无参数则按网页降级语义放行”。填入后重启即生效，**无需重新打包安卓 App**。

---

## 5. 安装部署步骤

```bash
# 1) 上传源码（zip 解压后的目录），进入目录
cd /www/teamcloud

# 2) 安装 Node 依赖（国内网络建议先配 npm 镜像）
npm config set registry https://registry.npmmirror.com
npm install

# 3) 设置环境变量（写入 /etc/environment 或 systemd/PM2 配置均可）
export TEAMCLOUD_S3_ACCESS_KEY='你的AK'
export TEAMCLOUD_S3_SECRET_KEY='你的SK'
# 可选：
export TEAMCLOUD_S3_ENDPOINT='https://cn-nb1.rains3.com'
export TEAMCLOUD_S3_BUCKET='teamcloud'

# 4) 准备数据库（非必须，sqlite 会自动建库，给足写权限即可）
touch teamcloud.db

# 5) 启动验证
PORT=9178 HOST=127.0.0.1 node server.js
# 看到日志：CC网盘服务器已启动: http://127.0.0.1:9178 即为成功
```

### 5.1 用 PM2 守护进程（推荐）

```bash
npm install -g pm2
export TEAMCLOUD_S3_ACCESS_KEY='...' TEAMCLOUD_S3_SECRET_KEY='...'
pm2 start server.js --name teamcloud --time
pm2 save
pm2 startup   # 按输出提示执行一次注册开机自启
```

> 环境变量也可写入 PM2：`pm2 env 0` / 在 `ecosystem.config.js` 中 `env: { ... }`。

---

## 6. Nginx HTTPS 反向代理（核心）

```nginx
server {
    listen 80;
    server_name www.dingandpay.xin;
    # 让 certbot 能自动签发，其余 http 跳 https
    location /.well-known/acme-challenge/ { root /var/www/certbot; }
    location / { return 301 https://$host$request_uri; }
}

server {
    listen 443 ssl http2;
    server_name www.dingandpay.xin;

    ssl_certificate     /etc/letsencrypt/live/www.dingandpay.xin/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/www.dingandpay.xin/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    # 上传/下载大文件，放开 body 大小（最大 2GB）
    client_max_body_size 2g;

    # 与 Node 长连接保持
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 120s;

    location / {
        proxy_pass http://127.0.0.1:9178;
    }
}
```

- **安卓 App 访问的是 `https://www.dingandpay.xin`**（代码内 `Constants.BASE_URL`），域名证书必须覆盖它。
- App 内已设置 `android:usesCleartextTraffic="false"`，且网络层会直接拒绝 `http://` 明文请求：**请务必先确保 443/HTTPS 可用，再升级客户端**。
- 如需在线文件下载走代理而非直连 S3，Node 提供 `/download/presign/:id` 返回 S3 预签名 HTTPS 地址（默认 S3 Endpoint 已是 HTTPS）。

---

## 7. 极验 / 人机验证配置

- Web 端：使用**第四代** `GEETEST_CAPTCHA_ID/KEY`（登录）与 `GEETEST_REGISTER_CAPTCHA_ID/KEY`（注册）。
- 安卓端：使用**行为验证3.0** `GT3_LOGIN_*`（登录）与 `GT3_REGISTER_*`（注册）。
- 若验证失败/未配置，服务端遵循“有参数则验证、无参数降级放行”的语义，不影响正常登录注册流程（安全级别降低，生产建议全部配置）。
- 需要服务器能访问极验接口：`api.geetest.com`（GT3 register.php/validate.php）与 `gcaptcha4.geetest.com`（v4 validate）。

---

## 8. 数据备份与升级

### 备份
```bash
# 数据库
cp teamcloud.db        /backup/teamcloud-$(date +%F).db
# 网盘文件均在对象存储桶内，另在云控制台做桶级快照/版本管理即可
```

### 升级
1. 用新源码 zip 覆盖（保留 `teamcloud.db`、`用户协议.txt`）。
2. `npm install` 更新依赖。
3. `pm2 restart teamcloud`（或重启 node）。
4. 冒烟：网页登录 → 上传/下载文件；安卓 App 登录 → 验证码弹窗 → 上传/下载文件。

---

## 9. 常见问题

| 现象 | 排查 |
|---|---|
| 安卓提示“已拒绝明文HTTP请求” | 有请求地址用了 http；确认 Nginx 只开 443，BASE_URL 为 https |
| 上传一直失败、报权限 | 确认 S3 桶策略与 AK/SK 有 `PutObject/HeadObject/DeleteObject/GetObject` 权限 |
| 人机验证一直“跳过” | 极验服务端校验未通过/未配置，检查对应 ID/KEY 与服务器出网 |
| 502 | Node 进程未启动或崩溃，看 `pm2 logs teamcloud`；全局错误已内置防崩处理 |

---

## 10. 安全建议

- 修改 `server.js` 中 session 密钥为随机值。
- 生产只开放 443；9178 仅监听 `127.0.0.1`。
- 定时更新系统安全补丁、使用高版本 TLS。
- 建议为 `teamcloud.db` 设置仅服务账户可读写。
