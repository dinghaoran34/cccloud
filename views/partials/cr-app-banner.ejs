<%
  /* 移动端顶部下载标语：点击下载安卓端 CC云盘 APK
     用法：移动版页面直接 include（始终显示）；
           现代外壳中请包在 .cr-app-banner-wrap 里（仅窄屏显示，桌面隐藏）。 */
%>
<style>
  .cr-app-banner {
    display: flex; align-items: center; justify-content: center; gap: 8px;
    min-height: 44px; padding: 12px 16px;
    background: linear-gradient(90deg, var(--cr-primary, #2b7de9), var(--cr-primary-hover, #1f6fd0));
    color: #fff; font-size: 15px; font-weight: 700; line-height: 1.3;
    text-decoration: none; box-shadow: 0 1px 3px rgba(16, 24, 40, .06);
  }
  .cr-app-banner:hover, .cr-app-banner:active { color: #fff; text-decoration: none; }
  .cr-app-banner:active { filter: brightness(.95); }
  .cr-app-banner-ico {
    display: inline-flex; align-items: center; justify-content: center;
    width: 20px; height: 20px; border-radius: 50%;
    background: rgba(255, 255, 255, .22); font-size: 11px;
  }
  /* 仅移动端显示（桌面端隐藏）：用于现代外壳页面 */
  .cr-app-banner-wrap { display: none; }
  @media (max-width: 768px) { .cr-app-banner-wrap { display: block; } }
</style>
<a class="cr-app-banner" href="/app/download">
  <span class="cr-app-banner-ico" aria-hidden="true">↓</span>
  <span>下载CC云盘</span>
</a>
